import 'package:flutter/material.dart';

class RecorderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Recorder Screen'),
      ),
      body: Center(
        child: Text('Hello world'),
      ),
    );
  }
}
